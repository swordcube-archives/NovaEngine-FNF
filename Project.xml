<?xml version="1.0" encoding="utf-8"?>
<project xmlns="http://lime.software/project/1.0.2" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://lime.software/project/1.0.2 http://lime.software/xsd/project-1.0.2.xsd">

	<!-- _________________________ Application Settings _________________________ -->

	<app title="FNF: Nova Engine" file="NovaEngine" main="Main" version="0.1.0" company="swordcube" />

	<!--The flixel preloader is not accurate in Chrome. You can use it regularly if you embed the swf into a html file
		or you can set the actual size of your file manually at "FlxPreloaderBase-onUpdate-bytesTotal"-->
	<app preloader="flixel.system.FlxPreloader" />

	<!--Minimum without FLX_NO_GAMEPAD: 11.8, without FLX_NO_NATIVE_CURSOR: 11.2-->
	<set name="SWF_VERSION" value="11.8" />

	<!-- ____________________________ Window Settings ___________________________ -->

	<!--These window settings apply to all targets-->
	<window width="1280" height="720" fps="1000" background="#000000" hardware="true" vsync="false" />

	<!--HTML5-specific-->
	<window if="html5" resizable="true" />

	<!--Desktop-specific-->
	<window if="desktop" orientation="landscape" fullscreen="false" resizable="true" allow-high-dpi="true" />

	<!--Mobile-specific-->
	<window if="mobile" orientation="landscape" fullscreen="true" width="0" height="0" />

	<!-- _____________________________ Path Settings ____________________________ -->

	<set name="BUILD_DIR" value="export/debug" if="debug"/>
	<set name="BUILD_DIR" value="export/release" unless="debug"/>

	<source path="source" />
	<assets path="assets" />
	<assets path="mods" />

	<!-- ____________________________ Custom Defines ____________________________ -->
	
	<!-- Comment this out to prevent unused classes from being compiled.
		 Reduces compilation time at the cost of limited HScript flexibility. !-->
	<define name="COMPILE_ALL_CLASSES" />

	<!-- Allows LUA scripts to be ran. -->
	<define name="LUA_ALLOWED" if="desktop" unless="web || hl || neko"/>

	<!-- Allows the "mods" folder to be used. -->
	<define name="MOD_SUPPORT" unless="web"/>

	<!-- Allows video cutscenes to work. -->
	<define name="VIDEO_CUTSCENES" unless="web || hl || mac"/>

	<!-- Allows Discord RPC to be used. -->
	<define name="DISCORD_RPC" unless="web || hl || neko"/>

	<!-- _______________________________ Libraries ______________________________ -->

	<haxelib name="hxcpp-debug-server" if="debug cpp"/>

	<haxelib name="flixel-leather" rename="flixel" />
	<haxelib name="hscript-improved" />
	<haxelib name="linc_luajit" if="LUA_ALLOWED"/>
	<haxelib name="discord_rpc" if="DISCORD_RPC" />
	<haxelib name="hxCodec" if="VIDEO_CUTSCENES" />

	<!-- ______________________________ Haxedefines _____________________________ -->

	<!--Optimise inputs, be careful you will get null errors if you don't use conditionals in your game-->
	<haxedef name="FLX_NO_MOUSE" if="mobile" />
	<haxedef name="FLX_NO_KEYBOARD" if="mobile" />
	<haxedef name="FLX_NO_TOUCH" if="desktop" />

	<!--Disable the Flixel core focus lost screen-->
	<haxedef name="FLX_NO_FOCUS_LOST_SCREEN" />

	<!--Disable the Flixel core debugger. Automatically gets set whenever you compile in release mode!-->
	<haxedef name="FLX_NO_DEBUG" unless="debug" />

	<!--Enable this for Nape release builds for a serious peformance improvement-->
	<haxedef name="NAPE_RELEASE_BUILD" unless="debug" />

	<!-- Used to make HScript work much better -->
	<haxedef name="hscriptPos" />

	<haxedef name="HXCPP_CHECK_POINTER" if="release" />
	<haxedef name="HXCPP_STACK_LINE" if="release" />
	<haxedef name="HXCPP_DEBUG_LINK" if="release" />

	<haxedef name="HXCPP_GC_BIG_BLOCKS" if="cpp" /> <!-- mmmm memory -->

	<haxedef name="no-deprecation-warnings" />

	<section if="COMPILE_ALL_CLASSES">
		<haxeflag name="-dce" value="no" />
		<haxeflag name="--macro" value="game.macros.ScriptsMacro.addAdditionalClasses()" />
	</section>

	<!-- ______________________________ Game Icons _____________________________ -->

	<icon path="art/icon16.png" size="16"/>
	<icon path="art/icon32.png" size="32"/>
	<icon path="art/icon64.png" size="64"/>
	<icon path="art/iconOG.png"/>
</project>
